# Model identifiers are settings, never secrets. Store the API key in the OS credential store.
ECHO_REASONING_MODEL=gpt-5
ECHO_STANDARD_MODEL=gpt-4.1
ECHO_FAST_MODEL=gpt-4.1-mini
ECHO_EMBEDDING_MODEL=text-embedding-3-small
